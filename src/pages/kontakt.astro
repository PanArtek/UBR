---
import Layout from '../layouts/Layout.astro';
import Navigation from '../components/Navigation.astro';
import ContactForm from '../components/ContactForm.astro';

const contactInfo = [
  {
    icon: '📞',
    title: 'Telefon',
    details: ['+48 XXX XXX XXX', 'Pon-Pt: 8:00-18:00', 'Sob: 9:00-14:00'],
  },
  {
    icon: '📧',
    title: 'Email',
    details: ['kontakt@ubr.pl', 'Odpowiadamy w ciągu 24h'],
  },
  {
    icon: '📍',
    title: 'Adres',
    details: ['ul. Przykładowa 123', '00-000 Warszawa', 'Mazowieckie'],
  },
];
---

<Layout title="Kontakt" description="Skontaktuj się z UBR. Otrzymaj darmową wycenę na budowę domu, remont mieszkania lub wykończenie wnętrz.">
  <Navigation />
  
  <!-- Page Header -->
  <section class="bg-gradient-to-r from-blue-600 to-blue-800 text-white py-12">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <h1 class="text-3xl md:text-4xl font-bold text-center">
        Skontaktuj się z nami
      </h1>
      <p class="text-xl text-center mt-4 max-w-2xl mx-auto">
        Jesteśmy gotowi odpowiedzieć na Twoje pytania i przygotować indywidualną wycenę
      </p>
    </div>
  </section>

  <!-- Contact Section -->
  <section class="py-16 bg-gray-50">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">
        <!-- Contact Form -->
        <div class="lg:col-span-2">
          <h2 class="text-2xl font-bold text-gray-900 mb-6">
            Formularz kontaktowy
          </h2>
          <ContactForm />
        </div>

        <!-- Contact Information -->
        <div class="space-y-6">
          <h2 class="text-2xl font-bold text-gray-900 mb-6">
            Dane kontaktowe
          </h2>
          
          {contactInfo.map((info) => (
            <div class="bg-white rounded-lg shadow-md p-6">
              <div class="flex items-start">
                <span class="text-3xl mr-4">{info.icon}</span>
                <div>
                  <h3 class="text-lg font-semibold text-gray-900 mb-2">
                    {info.title}
                  </h3>
                  {info.details.map((detail) => (
                    <p class="text-gray-600">{detail}</p>
                  ))}
                </div>
              </div>
            </div>
          ))}

          <!-- Business Hours -->
          <div class="bg-blue-50 rounded-lg p-6">
            <h3 class="text-lg font-semibold text-gray-900 mb-3">
              Godziny pracy
            </h3>
            <div class="space-y-1 text-gray-600">
              <p>Poniedziałek - Piątek: 8:00 - 18:00</p>
              <p>Sobota: 9:00 - 14:00</p>
              <p>Niedziela: Nieczynne</p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Map Section -->
  <section class="py-16 bg-white">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <h2 class="text-2xl font-bold text-gray-900 mb-6 text-center">
        Znajdź nas
      </h2>
      <div class="bg-gray-200 rounded-lg h-96 flex items-center justify-center">
        <p class="text-gray-600">Miejsce na mapę Google</p>
      </div>
    </div>
  </section>

  <!-- Footer -->
  <footer class="bg-gray-900 text-white py-8">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="flex flex-col md:flex-row justify-between items-center">
        <div class="mb-4 md:mb-0">
          <span class="text-2xl font-bold">UBR</span>
          <p class="text-gray-400">Profesjonalne usługi budowlane</p>
        </div>
        <div class="text-center md:text-right">
          <p class="text-gray-400">&copy; 2025 UBR. Wszystkie prawa zastrzeżone.</p>
        </div>
      </div>
    </div>
  </footer>
</Layout>